version: "3.9"

services:
  voicevox-android-build:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: voicevox_android_build
    volumes:
      - ./../:/workspace
    working_dir: /workspace
    tty: true
